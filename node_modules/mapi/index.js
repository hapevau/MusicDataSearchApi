var searcher = require('./lib/search/apisearchrequester.js').masr,
    names = require('./lib/base/names.js'),
    search1 = 'katzenjammer',
    search2 = 'Deep Purple',
    r1 = searcher.getRequest(names.DEWIKINAME, search1),
    r2 = {};
  

// Verwendung:
searcher.setRequestsSearch(search2);
r2 = searcher.getRequest(names.ENWIKINAME);

function cb(e, data) {
    console.log('=====================================================================');
    console.log((e) ? e : data);
    console.log('=====================================================================');
}

r1.makeRequest(cb);
r2.makeRequest(cb);

